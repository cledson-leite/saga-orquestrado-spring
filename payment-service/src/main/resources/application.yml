server:
  port: 8083

spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/saga_payment
    username: postgres
    password: postgres

  database:
    driverClassName: org.postgresql.Driver
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
